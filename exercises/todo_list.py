# exercises/todo_list.py

def pending_tasks(tasks: list[str], completed_indices: list[int]) -> list[str]:
    """
    Given a list of tasks and a list of indices (0-based) that are completed,
    return only the tasks whose indices are not in completed_indices.
    """
    raise NotImplementedError() 