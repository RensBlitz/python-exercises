# exercises/leaderboard_manager.py

def update_top_three(current: list[int], new_score: int) -> list[int]:
    """
    Given current top-3 scores (descending) and a new_score,
    return an updated top-3 list (descending). If new_score doesn't make the top-3, return current.
    """
    raise NotImplementedError() 