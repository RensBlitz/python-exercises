# exercises/temperature_spread.py

def spread_and_average(high: float, low: float) -> tuple[float, float]:
    """
    Returns (difference, average):
      difference = high - low
      average = (high + low) / 2
    """
    raise NotImplementedError() 