# exercises/amusement_park_ticket.py

def compute_price(age: int, base_price: float) -> float:
    """
    - If age < 18: free (0.0)
    - If age ≥ 65: free (0.0)
    - Otherwise: pay base_price
    """
    raise NotImplementedError() 