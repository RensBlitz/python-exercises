# exercises/order_processor.py

def total_with_free_shipping_for_big_orders(orders: list[float]) -> float:
    """
    If sum(orders) ≥ 100, return sum(orders), else return 0.
    """
    raise NotImplementedError() 