# exercises/simple_dungeon_crawler.py

def simulate(actions: list[str]) -> int:
    """
    Start with health = 5. For each action:
      - "hit"   → health -= 1
      - "potion" → health += 1
    Return final health.
    """
    raise NotImplementedError() 