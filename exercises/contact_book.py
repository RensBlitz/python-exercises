# exercises/contact_book.py

def to_map(contacts: list[tuple[str, str]]) -> dict[str, str]:
    """
    Given a list of (name, phone) tuples, return a dict mapping name → phone.
    """
    raise NotImplementedError() 